# Third-party notices

This project uses published package APIs and does not copy source code from the projects below.

- Firecrawl hosted API: this project uses its public API and does not include or self-host the Firecrawl server.
- LangGraph, Next.js, React, Zod, Vitest, Playwright and their transitive dependencies: see their respective package licenses in `node_modules` and the lockfile.

Before redistributing, review the resolved dependency licenses for the exact release you ship.
