# Third-party notices

This project uses published package APIs and does not copy source code from the projects below.

- Firecrawl SDK (`firecrawl`): client dependency for the hosted API. The Firecrawl server is AGPL-3.0; this Demo does not include or self-host its server code.
- `better-sqlite3`, Next.js, React, Zod, Vitest, Playwright and their transitive dependencies: see their respective package licenses in `node_modules` and the lockfile.

Before redistributing, review the resolved dependency licenses for the exact release you ship.
